[ЗАКАЗЧИК]
Страница хорошая, но люди спрашивают, как у нас всё устроено. Добавьте блок «Как мы работаем» — три шага, коротко.

---

[ЛОГИКА]
Это самая частая работа в вёрстке: не придумать новое, а **дописать в существующем стиле**. Значит, сначала читаем, что уже есть.

Открой `index.html` и посмотри на секцию `.features`: заголовок `<h2>`, контейнер `.cards`, внутри три `<article class="card">` с `<h3>` и `<p>`. В `style.css` эти классы уже описаны — новые писать не нужно.

---

[ЛОГИКА]
Что нужно сделать:

1. Добавь `<section class="steps">` между секцией преимуществ и подвалом.
2. Внутри — заголовок `<h2>Как мы работаем</h2>`.
3. Дальше `<div class="cards">` и три карточки `.card`, у каждой `<h3>` и `<p>`.
4. Пронумеруй шаги: «1. …», «2. …», «3. …».

Текст придумай сам — это твоя кофейня.

---

[КУРСОР]
Если всё сделано правильно, CSS трогать не придётся вообще: карточки сами встанут в ряд, потому что стили уже написаны для этих классов. В этом и смысл — переиспользовать, а не плодить новое.
