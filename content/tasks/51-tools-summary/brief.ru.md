[КУРСОР]
Последняя глава пройдена. Ты умеешь всё, что нужно, чтобы довести проект от пустой папки до ссылки, которую можно кому-то отправить.

Осталось сделать это одним заходом. Без подсказок по шагам — их в этой главе было достаточно.

---

[ТЕРМИНАЛ]
Ты стоишь в домашней папке. Здесь пусто: ни проекта, ни репозитория, ничего.

```
$ ls
(пусто)
```

Дальше — сам.

---

[ЛОГИКА]
## Что должно получиться

- Проект живёт в собственной папке, а не вперемешку с загрузками.
- Внутри — структура, а не куча файлов: точка входа, README для человека, файлы по папкам.
- Репозиторий создан, и в истории **хотя бы два коммита** с осмысленными сообщениями.
- Главная ветка называется `main`.
- Настроен адрес репозитория, и код отправлен на GitHub.

Как назвать проект и что в нём будет — решаешь ты.

---

[ПАМЯТЬ]
Два коммита — не придирка. Смысл git не в том, чтобы один раз сохранить всё в конце, а в том, чтобы можно было вернуться к любому шагу. Сделал кусок работы — зафиксировал.

И про ветку: git до сих пор создаёт `master` по умолчанию, но договорённость в мире давно другая. Переименовывать привычнее до первого пуша, а не после.

---

[КУРСОР]
Проверки смотрят на итоговое состояние, а не на список команд. Если пойдёшь другим путём и придёшь туда же — задание засчитается.

Забыл команду — `help` покажет весь список.
