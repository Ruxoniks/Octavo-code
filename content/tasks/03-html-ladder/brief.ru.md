[КУРСОР]
Мне нужно наверх — там кнопка, которую очень хочется нажать. Лесенку строишь ты.

---

[ЛОГИКА]
В файле `style.css` уже описано, где какая ступенька находится:

```css
.step--1 { bottom: 20px;  left: 20px;  }
.step--2 { bottom: 90px;  left: 100px; }
.step--3 { bottom: 160px; left: 180px; }
```

и так далее до пятой. То есть **класс решает, где окажется ступенька**. Твоя задача — создать сами ступеньки. Файл со стилями можно открыть и поменять — проверка смотрит только на разметку, так что экспериментируй смело.

---

[ЛОГИКА]
Внутри `<div class="ladder">` добавь **пять** ступенек:

```html
<div class="step step--1">1</div>
```

У каждой два класса через пробел: `step` — общий вид, `step--N` — место на экране. Номера идут подряд с 1 до 5.

---

[КУРСОР]
Учти: я дотягиваюсь максимум на 90 пикселей вверх. Пропустишь третью ступеньку — между второй и четвёртой окажется 140, и я застряну на полпути.

Проверка так и скажет: до какой ступеньки я добрался и почему дальше не смог. Когда всё сойдётся — жми «Проверить» и смотри, как я лезу.
